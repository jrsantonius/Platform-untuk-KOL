// Scheduler dihandle oleh Vercel Cron Jobs (lihat vercel.json)
// File ini tidak dipakai lagi, disimpan untuk referensi saja.
export function startScheduler(_baseUrl: string) {
  console.log("[Scheduler] Using Vercel Cron — no local scheduler needed.");
}
